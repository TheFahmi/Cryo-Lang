fn main() {
    // Marker for start (0)
    print(0);
    
    let sum = 0;
    let i = 0;
    while (i < 1000000) {
        sum = sum + 1;
        i = i + 1;
    }
    
    // Check result
    print(sum);
}
