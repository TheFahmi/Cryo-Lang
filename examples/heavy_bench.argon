// heavy_bench.argon
// Heavy benchmark: Loop 100,000 iterations

fn main() {
    let count = 0;
    let sum = 0;
    
    // Loop 100,000 times
    while (count < 100000) {
        sum = sum + count;
        count = count + 1;
    }
    
    print(sum);
    print(count);
}
