// million_bench.argon 
// Heavy benchmark: Loop 1,000,000 iterations

fn main() {
    let count = 0;
    let sum = 0;
    
    while (count < 1000000) {
        sum = sum + count;
        count = count + 1;
    }
    
    print(sum);
    print(count);
}
