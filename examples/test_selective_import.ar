// Test Selective Module Import
import {math_add, math_square} from "examples/math_utils.ar";

fn main() {
    print("Testing Selective Imports...");
    
    // These should work (imported)
    let sum = math_add(10, 20);
    print("math_add(10, 20) = " + sum);
    
    let sq = math_square(5);
    print("math_square(5) = " + sq);
    
    // math_mul was NOT imported, so this would fail if uncommented:
    // let prod = math_mul(3, 4);
    
    print("Selective import test complete!");
}
